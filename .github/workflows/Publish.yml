name: Project Publishing
permissions:
  contents: read
on:
  push:
    paths: [ 'cat2/**','**/Publish.yml' ]
jobs:
  publish:
    strategy:
      matrix:
        config:
          - name: "single-file"
            args: "-p:PublishSingleFile=true -p:IncludeAllContentForSelfExtract=true -p:SatelliteResourceLanguages=."
          - name: "ready-to-run"
            args: "-p:PublishReadyToRun=true -p:PublishReadyToRunComposite=true"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@main
      - uses: actions/cache@main
        with:
          path: ~/.nuget/packages
          key: nuget-cache
      - run: dotnet publish -r win-x64 ${{ matrix.config.args }}
      - uses: actions/upload-artifact@main
        with:
          name: Release-${{ matrix.config.name }}
          path: |
             **/publish
             !**/*.pdb
